#-----------------------------------------------------------------------------
# Config file for compiling against the build directory
#-----------------------------------------------------------------------------
get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

#-----------------------------------------------------------------------------
# Directories
#-----------------------------------------------------------------------------
set(HERMES_VFD_INCLUDE_DIR "@HERMES_VFD_INCLUDES_BUILD_TIME@")

#-----------------------------------------------------------------------------
# Version Strings
#-----------------------------------------------------------------------------
set(HERMES_VFD_VERSION_STRING  @HERMES_VFD_PACKAGE_VERSION@)
set(HERMES_VFD_VERSION_MAJOR   @HERMES_VFD_VERSION_MAJOR@)
set(HERMES_VFD_VERSION_MINOR   @HERMES_VFD_VERSION_MINOR@)
set(HERMES_VFD_VERSION_PATCH   @HERMES_VFD_VERSION_PATCH@)

#-----------------------------------------------------------------------------
# Don't include targets if this file is being picked up by another
# project which has already built HERMES as a subproject
#-----------------------------------------------------------------------------
if(NOT TARGET "hermes_vfd" AND NOT HERMES_VFD_INSTALL_SKIP_TARGETS)
  include(${SELF_DIR}/hermes_vfd-targets.cmake)
  if(NOT TARGET "hermes")
    include(@HERMES_DIR@/hermes-config.cmake)
  endif()
endif()
